to generate ui file use ```pyside6-uic``` tool

```
pyside6-uic <ui file> -o <output file>
```

for example (running from main directory)

```
pyside6-uic gui/mainwindow.ui -o gui/generated/mainwindow.py  
```